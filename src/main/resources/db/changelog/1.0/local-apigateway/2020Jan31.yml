databaseChangeLog:
  - changeSet:
      id: '31Jan2020-151700'
      author: khanh_nguyen_vnn
      dbms: 'postgresql'
      rollback:
        - sql:
            sql:
                DELETE FROM orbitmaster.configuration WHERE application = 'local-api-gateway' and 'profile' = 'default' and label = 'default' and key='authenticate.partner-service-endpoint';
      changes:
        - sql:
            sql:
              INSERT INTO orbitmaster.configuration(application, profile, label, key, value) VALUES ('local-api-gateway', 'default', 'default', 'authenticate.partner-service-endpoint', '${ds}{services.service-endpoint.partner-management}/internal/partners');

